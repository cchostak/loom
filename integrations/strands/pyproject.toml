[project]
name = "loom-strands"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = ["strands-agents==1.57.0", "mcp==1.30.0", "httpx==0.28.1", "cryptography==50.0.1", "jsonschema==4.26.0"]

[dependency-groups]
dev = ["pytest==8.4.2", "ruff==0.15.7"]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
pythonpath = ["."]
